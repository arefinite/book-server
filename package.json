{
  "name": "book-management-app",
  "version": "1.0.0",
  "description": "api for book management app",
  "main": "server.js",
  "author": "arefinite",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.4",
    "morgan": "^1.10.0"
  }
}
